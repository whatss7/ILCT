#include <stdio.h>
void debugp(const char *fmt, ...) {}
typedef long long ll;
////////////////////////
int main() {
	int cn;
	scanf("%d", &cn);
	while (cn--) {
		int n;
		scanf("%d", &n);
	}
	return 0;
}
